# CLAUDE.md

This file imports the nearest AGENTS.md. See `src/providers/jcode/AGENTS.md` for the execution context for this scope.

`src/providers/jcode/` adapts the Jcode CLI (`jcode acp`) as a Claudian provider, following the ACP subprocess pattern of the opencode provider. Read `src/providers/opencode/AGENTS.md` too; the jcode guide only states jcode-specific deviations and ownership.
